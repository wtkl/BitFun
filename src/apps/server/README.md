# BitFun Server (Web App Backend)

This directory contains the `bitfun-server` application.

If you are looking for **Remote Connect self-hosted relay deployment**, use:

- `src/apps/relay-server/README.md`
- `src/apps/relay-server/deploy.sh`

`src/apps/server` and `src/apps/relay-server` are different components.
